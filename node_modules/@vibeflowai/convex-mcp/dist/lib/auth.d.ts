import type { BearerAuthConfig, McpHttpOptions } from "./types.js";
export declare function bearerAuth(options: {
    env: string;
    optional?: boolean;
}): BearerAuthConfig;
export declare function enforceAuth(options: McpHttpOptions | undefined, request: Request): Response | null;
